# ==============================================================================
# SAM (Smart Autonomous Manager) - Environment Configuration
# ==============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!
# ==============================================================================

# ------------------------------------------------------------------------------
# Database Configuration
# ------------------------------------------------------------------------------
# PostgreSQL connection string (Neon, cPanel PostgreSQL, or self-hosted)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require
DATABASE_URL="postgresql://user:password@localhost:5432/sam_production?sslmode=require"

# ------------------------------------------------------------------------------
# Anthropic AI (Claude) Configuration
# ------------------------------------------------------------------------------
# Get your API key from: https://console.anthropic.com/
# Required for conversational AI features
ANTHROPIC_API_KEY="sk-ant-api03-your-key-here"

# ------------------------------------------------------------------------------
# ElevenLabs Text-to-Speech Configuration
# ------------------------------------------------------------------------------
# Get your API key from: https://elevenlabs.io/
# Required for voice synthesis
ELEVENLABS_API_KEY="sk_your-elevenlabs-key-here"

# Voice ID for Arabic speech synthesis
# Default: v0GSOyVKHcHq81326mCE (Arabic female voice)
# Find more voices at: https://elevenlabs.io/voice-library
ELEVENLABS_VOICE_ID_AR="v0GSOyVKHcHq81326mCE"

# ------------------------------------------------------------------------------
# Application Configuration
# ------------------------------------------------------------------------------
# Base URL for your application (used for redirects, webhooks, etc.)
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Node environment (auto-set by Next.js, but can override)
# Options: development | production | test
NODE_ENV="production"

# ------------------------------------------------------------------------------
# Optional: Additional Configuration
# ------------------------------------------------------------------------------
# Uncomment and configure as needed:

# API Rate Limiting (requests per minute)
# RATE_LIMIT_RPM=60

# Maximum concurrent conversations
# MAX_CONCURRENT_SESSIONS=100

# Session timeout (minutes)
# SESSION_TIMEOUT_MINUTES=30

# Log level (error | warn | info | debug)
# LOG_LEVEL="info"

# ==============================================================================
# SECURITY NOTES:
# 1. Rotate API keys if .env is ever exposed
# 2. Use strong database passwords (16+ characters, mixed case, symbols)
# 3. Enable SSL/TLS for database connections in production
# 4. Store secrets in cPanel environment variables instead of .env file
# ==============================================================================
